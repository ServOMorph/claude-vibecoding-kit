# Contexte — claude-vibecoding-kit

## Objectif (immuable sauf décision explicite)
Fournir un kit reproductible pour gérer le vibecoding sur des projets multi-sessions, avec contexte persistant via `/start`/`/close` et support multi-zones.

## Stack / contraintes techniques
- **Langage** : Markdown + Bash/PowerShell pour scripts
- **Framework** : Claude Code CLI + Agent SDK
- **Gestion git** : commits automatiques depuis `/close`
- **Modèles recommandés** : Haiku (start), Sonnet (close), Opus (plans/debug)
- **Intégration** : Ollama pour tâches sensibles/templated
- **Déploiement** : copie template vers projets via `/init`, tracking dans DEPLOYMENTS.md

## État actuel
- Prototype "assistant vocal distant pour Claude Code" (`templates/roberto/com_telephone/voice-code-bridge/`) fonctionnel de bout en bout, réponses reformulées courtes/orales et débit TTS ralenti, UI mobile protégée par token (`AUTH_TOKEN` dans `server/.env`). Reste : boucle de déclenchement micro rapproché à diagnostiquer.
- Workflow `quotidien` (`AUTOMATISATIONS/workflows/quotidien/`) opérationnel, `decisions.md` désormais une roadmap persistante à cases à cocher ; première revue réelle amorcée.
- `/start` de la zone globale du kit jugé surchargé par l'utilisateur (2026-08-20) — allègement à trancher en priorité avant la prochaine session.
- Mission ROBERTO (`Appli_TSA_SDI_TDAH`, hors dépôt kit) en pause depuis le 2026-08-18, reprise prévue sur la zone globale du projet, pas sur la zone `roberto`.
- Kit v3.36 : `/create_memory` scopé par zone et `/init_discord_mode` validée en conditions réelles (v3.30/v3.31) restent non/peu testés à grande échelle. Détail complet dans `CHANGELOG.md`.

## Décisions structurantes
_Décisions antérieures au 2026-08-17 archivées dans `_contexte/archive_decisions.md`._
- 2026-08-20 : Authentification par token ajoutée à l'UI mobile du prototype "assistant vocal" (`voice-code-bridge/server.js`) — cookie posé via `?token=` en première visite, WebSocket vérifié via `verifyClient`, `/send` (appelé par l'agent local) restreint à `127.0.0.1` sans token. Token stocké dans `server/.env` (gitignoré) plutôt qu'en variable d'environnement système : `setx` s'est révélé peu fiable (ne se propage pas à la session VSCode/Claude Code déjà ouverte, même après reload window). Limite documentée pour l'utilisateur : le token protège l'accès, pas les actions que l'agent exécutera ensuite sur la base des messages reçus.
- 2026-08-20 : `decisions.md` du workflow `quotidien` ne s'archive/réinitialise plus à chaque lancement (roadmap vivante à cases à cocher, cochée seulement après validation explicite de l'utilisateur) — l'ancien mécanisme risquait de perdre des tâches non finies en cas de session interrompue. Réponses vocales (`POST /send`) désormais reformulées courtes/orales (règle ajoutée à `com_telephone/README.md`) et débit TTS ralenti (`length_scale=1.25`), sur demande explicite de l'utilisateur (écoute, pas lecture).
- 2026-08-19 : Pilotage du prototype "assistant vocal Claude Code" centralisé dans un script unique (`com_telephone/_commands/com_manager.py`, start/stop/restart/status des 3 process) plutôt que 3 commandes manuelles séparées — bug réel trouvé en le testant (le lanceur `py -3.11` spawn un processus enfant distinct, `taskkill` sans `/T` laissait le vrai serveur orphelin). La commande Claude Code associée (`com_manager.md`) active aussi automatiquement la surveillance de `messages.log`, le watcher ne survivant pas à un changement de session contrairement aux process serveur eux-mêmes (constaté : messages reçus mais non vus tant que la surveillance n'est pas relancée). UI vocale enrichie (état "réflexion" orange, pause micro) à la demande de l'utilisateur.
- 2026-08-19 : Prototype "assistant vocal distant pour Claude Code" (`templates/roberto/com_telephone/voice-code-bridge/`) construit selon une spécification fournie (PDF) : PWA + WebSocket + STT/TTS locaux plutôt que cloud (stratégie gratuite retenue). Web Speech API du navigateur écartée en cours de route (non supportée sur Safari/iOS) au profit de `MediaRecorder` + Whisper local (faster-whisper, déjà installé) côté serveur. TTS via Piper local (voix `fr_FR-siwis-medium`, déjà installé). Accès distant via un tunnel Cloudflare déjà configuré par l'utilisateur (`vertia-test.serenia-tech.fr`) plutôt qu'un nouvel hébergement — proposition d'héberger l'UI sur le site de production SérénIATech écartée (risque de mélanger un outil de dev expérimental avec un site commercial). Le traitement des messages reste manuel (Claude Code, via un `Monitor` sur `messages.log`), confirmé suffisant par l'utilisateur plutôt qu'un branchement automatisé.
- 2026-08-18 : Lecture croisée accordée à la zone `roberto` (`Appli_TSA_SDI_TDAH`) vers `_contexte/signals.md`/`contexte.md` racine (lecture seule), après constat que la zone travaillait sans le contexte global du projet. Fusion des deux zones écartée (casserait l'isolation de contexte voulue par le système de zones) au profit d'une lecture ciblée. Problème réel découvert au passage : une session `/close roberto` avait écrit sa synthèse dans les fichiers `_contexte` racine au lieu de ceux de la zone, mémoire figée depuis la création de l'agent — corrigé. Mission mise en pause, reprise prévue sur la zone globale. Généralisation du pattern au kit non tranchée.
- 2026-08-18 : Mission ROBERTO (`Appli_TSA_SDI_TDAH`) réorientée après analyse a posteriori du relai `chatgpt-orchestrateur` (composer une délégation avec code de référence inline coûte autant que l'écrire directement, captures d'écran en surcoût net) — plus-value retenue : test d'autonomie, journalisation, pas l'économie de tokens. Zone-agent `roberto` créée via `/create_agent` (mode création sur dossier déjà riche en contenu, périmètre étendu à `scripts/`/`_contexte/` racine). Incohérence détectée dans `agent_role_TEMPLATE.md` (section Invariants fixe contredisant tout périmètre étendu) — corrigée à la main, proposition P14 tracée pour paramétrer le template.
- 2026-08-18 : Roadmap `roadmap_reprise_multicompte.md` créée et Phases 1/2 terminées : suivi de tokens estimés par agent (`usage.json`) et par compte Google (`comptes_usage.json`/`compte_actif.json`, statut actif/inactif/epuise), `comptes_google.json` (13 comptes autorisés, mapping profil Chrome), `ouvrir_nouveau_compte.ps1` (ouverture automatique round-robin). Risque de vol de focus (`SetForegroundWindow` peut échouer silencieusement, le clic part vers une autre fenêtre) découvert et documenté dans `SKILL.md`, bloquant avant la Phase 3. En parallèle, délégation réelle testée sur la mission ROBERTO (`Appli_TSA_SDI_TDAH`) : Flux A de la state machine (testeur Marie) implémenté et testé de bout en bout par ChatGPT (10 étapes, 29 tests verts), Claude Code se limitant à appliquer/vérifier — validation grandeur nature du principe de délégation.
- 2026-08-18 : `skills/chatgpt-orchestrateur/` généricisé (paramètre `-Agent`, plusieurs IA orchestratrices possibles, ChatGPT en premier) et doté d'un journal léger (`log.jsonl`, une ligne/échange) pour analyse ultérieure de la communication par agent. Gestion de l'état exclusivement via 5 scripts PowerShell dédiés (`init_agent`, `maj_etat`, `log_echange`, `generer_reprise`, `coller_et_envoyer`), jamais par écriture manuelle — économie de tokens. `coller_et_envoyer.ps1` automatise l'envoi (clic + collage + Entrée dans la fenêtre orchestrateur, ciblée par position écran), testé en conditions réelles avec succès ; presse-papier reste le repli si la fenêtre n'est pas trouvée. Règle actée : les réponses de l'orchestrateur doivent être en un seul bloc Markdown sans commentaire hors bloc. Bug corrigé : encodage UTF-8 non forcé sur `Get-Content -Raw`.
- 2026-08-18 : mémoire scopée par zone ajoutée à `/create_memory` : `[alias_zone] [contenu]` écrit dans `<dossier_zone>/_contexte/memory.md` si le premier mot correspond à un alias de `.claude/zones.md`, sinon comportement global inchangé (`.claude/memory.md`). `/start` charge la mémoire de zone résolue (étape 2c). Demande initiale : mémoire dédiée à la zone `linkedin` de SérénIATech_dev — implémenté au niveau kit (approche retenue par l'utilisateur parmi 3 options), jamais testé en conditions réelles, nécessite un `/update` sur ce projet. `Protocole_start_close_context.md`, `README.md`, `CHANGELOG.md` synchronisés. Kit v3.31.
- 2026-08-18 : `/init_discord_mode` testée en conditions réelles pour la première fois (ROBERTO, `C:\Users\raph6\Documents\SerenIATech\SérénIATech_dev\ROBERTO`) : insertion + configuration (token, Application ID/URL OAuth2, Message Content Intent, channel_id) + validation (`bot_manager.py status`, `!ping`) confirmées par l'utilisateur. Faille de sécurité détectée en cours de test : le workflow initial faisait passer le bot token (secret) par Claude, lu et écrit dans `config_bot_discord.json`. Corrigée : token déplacé dans `discord_com/.env` (créé/rempli par l'utilisateur lui-même, jamais lu ni écrit par Claude — vérifications de présence uniquement via commande shell à sortie booléenne) ; `config_bot_discord.json` réduit à `enabled`/`channel_id`. Fichiers mis à jour : `bot.py` (lecture via `os.environ`/`python-dotenv`), `requirements.txt`, `config_bot_discord.example.json`, `.env.example` (nouveau), `SETUP.md`, `DISCORD_SECURITY.md`, `.claude/commands/init_discord_mode.md`. Token exposé pendant le test régénéré par l'utilisateur côté Developer Portal. Bot de test antérieur oublié ("vibecoder", PID local, `discord_com/bot.py` racine du kit) repéré et arrêté. Kit v3.30.
